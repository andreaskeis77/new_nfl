# Project State

## Current phase

**T2.6G Provenance-Drilldown** — abgeschlossen (2026-04-23). Nächster Bolt: T2.6H (Run-Evidence-Browser).

## Architektur-Baseline (freigegeben am 2026-04-13)

- `concepts/NEW_NFL_SYSTEM_CONCEPT_v0_3.md` — verbindlicher Architektur-Anker
- `ENGINEERING_MANIFEST_v1_3.md` — verbindliche Engineering-Regeln
- `UI_STYLE_GUIDE_v0_1.md` — verbindliche UI-Regeln
- `T2_3_PLAN.md` — aktiver Tranche-Plan zum v1.0-Ziel (Ende Juni 2026)
- `USE_CASE_VALIDATION_v0_1.md` — abgenommene Use Cases
- ADR-0025, ADR-0026, ADR-0027, ADR-0028, ADR-0029, ADR-0030, ADR-0031 — `Accepted`; ADR-0032 (bitemporale Roster-Modellierung) `Proposed` seit T2.5D
- `CHAT_HANDOFF_PROTOCOL.md`, `LESSONS_LEARNED_PROTOCOL.md` — Methode
- Letzter Chat-Handoff: `docs/_handoff/chat_handoff_20260422-1500_t25c-players-and-first-real-dedupe-done.md`

## Completed

- A0 architecture and method foundation
- T1.0 local bootstrap
- T1.1 metadata registry
- T1.2 adapter skeleton
- T1.3 first fetch contract
- T1.4 first true remote fetch
- T1.5 first normalized staging load for `nflverse_bulk`
- T1.5 repair 01: remove the `stage_load` circular export and restore the import/collection gate
- T1.x method hardening for artifact apply and validation workflow
- T2.0 entry cycle cut
- T2.0A first canonical dictionary core load
- T2.0B methodik bolt for separation of `Einordnung` and `Aktion`
- T2.0C first browseable core dictionary slice
- T2.0D exact core dictionary field lookup
- T2.0E negative lookup miss-path hardening
- T2.0F core dictionary browse with `data_type` filter
- T2.0G core dictionary summary by `data_type`
- T2.1A stage-load source-file pinning
- T2.1B source-file discovery for operators
- T2.1C local HTML preview for core dictionary
- T2.1D local mini webserver for preview
- T2.3A Job-/Run-Modell-Skeleton (meta.job_definition, job_schedule, job_queue, job_run, run_event, run_artifact, retry_policy + Pydantic-Modelle + CLI `list-jobs`/`describe-job`/`register-job`/`register-retry-policy`)
- T2.3B Internal Runner (`src/new_nfl/jobs/runner.py`: atomarer Claim, Executor-Registry, Retry-Policy-Auswertung, Replay; geteilter DB-Helper `src/new_nfl/_db.py`; CLI `run-worker --once|--serve` und `replay-run`; `fetch-remote`/`stage-load` laufen nur noch über den Runner)
- T2.3C Quarantäne-Domäne (`src/new_nfl/jobs/quarantine.py`: `meta.quarantine_case`, `meta.recovery_action`; Dedupe per `(scope_type, scope_ref, reason_code)`; Auto-Quarantäne-Hook im Runner bei `runner_exhausted`; CLI `list-quarantine`, `quarantine-show`, `quarantine-resolve --action replay|override|suppress`; Replay erzeugt neuen `job_run_id` und schließt den Case bei Erfolg)
- T2.3D Read-Modell-Trennung (`src/new_nfl/mart/`: `mart.schedule_field_dictionary_v1` als versionierte Read-Projektion über `core.schedule_field_dictionary`; Runner-Executor `mart_build`; CLI `mart-rebuild --mart-key …`; `core-load --execute` triggert Mart-Build implizit; `core_browse`/`core_lookup`/`core_summary`/`web_preview`/`web_server` lesen ausschließlich aus `mart.*`; Lint-Test verbietet `core.*`/`stg.*`/`raw/` in Read-Modulen)
- T2.3E ADR-Block aktualisiert (`docs/adr/README.md` als vollständiger Index mit Status + Tranchen-Anker für ADR-0001 bis ADR-0030; ADR-0025/0028/0029 final `Accepted`; ADR-0026/0027/0030 bleiben `Proposed` bis zur jeweiligen Tranche T2.4A/T2.4B/T2.6A)
- T2.4A Ontology-as-Code-Skelett (`ontology/v0_1/term_*.toml`, `src/new_nfl/ontology/loader.py` mit `content_sha256`-Idempotenz; `meta.ontology_version`/`ontology_term`/`ontology_alias`/`ontology_value_set`/`ontology_value_set_member`/`ontology_mapping`; CLI `ontology-load`, `ontology-list`, `ontology-show --term-key <key|alias>`; Pydantic-Service `load_ontology_directory`/`list_terms`/`describe_term`; ADR-0026 final `Accepted`, TOML-Format statt YAML)
- T2.4B Dedupe-Pipeline-Skelett (`src/new_nfl/dedupe/` mit fünf Stufen `normalize → block → score → cluster → review` und `pipeline.py`; `meta.dedupe_run` + `meta.review_item`; `RuleBasedPlayerScorer` mit sechs Score-Stufen; `Scorer`-Protocol für spätere ML-Erweiterung; CLI `dedupe-run --domain players --demo` und `dedupe-review-list`; Demo-Set deckt Auto-Merge, Review und No-Match in einem Lauf; ADR-0027 final `Accepted`)
- T2.5A Teams-Domäne (Adapter-Slice-Registry `src/new_nfl/adapters/slices.py`; `core.team` mit Tier-A/Tier-B-Konflikt-Erkennung und automatischem `meta.quarantine_case`-Öffnen per `reason_code='tier_b_disagreement'`; Tier-A gewinnt nach ADR-0007; Read-Modell `mart.team_overview_v1`; Ontology-Terme `conference` + `division` ergänzt; CLI `--slice`-Flag für `fetch-remote`/`stage-load`/`core-load`; ADR-0031 `Proposed`)
- T2.5B Games-Domäne (Slices `(nflverse_bulk, games)` primär und `(official_context_web, games)` als Cross-Check; `core.game` mit `game_id`-Deduplikation via `ROW_NUMBER() OVER (PARTITION BY LOWER(TRIM(game_id)))`, Tier-A/B-Konflikt-Erkennung auf `home_score`, `away_score`, `stadium`, `roof`, `surface` und automatischem `meta.quarantine_case`-Öffnen; Read-Modell `mart.game_overview_v1` mit abgeleitetem `is_completed` und `winner_team` (`home_team`/`away_team`/`TIE`/NULL); CLI `list-slices` für Operator-Sicht über `SLICE_REGISTRY`; erste reale HTTP-Runde für `official_context_web` via stdlib-`ThreadingHTTPServer` end-to-end in `urllib.request.urlopen`-Pfad; ADR-0031 final `Accepted`)
- T2.5C Players-Domäne + erste echte Dedupe-Anwendung (Slices `(nflverse_bulk, players)` primär und `(official_context_web, players)` als Cross-Check; `core.player` ([src/new_nfl/core/players.py](../src/new_nfl/core/players.py)) mit `UPPER(TRIM(player_id))`-Kanonicalisierung, TRY_CAST für `birth_date`, `height`, `weight`, `rookie_season`, `last_season`, `jersey_number`, `draft_year`/`round`/`pick`, UPPER für `position`/`current_team_id`/`draft_club`; Tier-A-Dominanz, Tier-B-Diskrepanzen auf `display_name`, `position`, `current_team_id`, `jersey_number` öffnen `meta.quarantine_case` mit `scope_type='player'`; Read-Modell `mart.player_overview_v1` ([src/new_nfl/mart/player_overview.py](../src/new_nfl/mart/player_overview.py)) mit abgeleitetem `full_name` (Fallback `first_name || ' ' || last_name`), `is_active = (last_season IS NULL)` und best-effort `position_is_known` aus der aktiven Ontologie-Version (`NULL` wenn keine aktive Version geladen); Brücke [src/new_nfl/dedupe/core_player_source.py](../src/new_nfl/dedupe/core_player_source.py) liest `core.player` direkt in den T2.4B-Dedupe-Pipeline-Input, CLI `dedupe-run --domain players --source core-player` als erste reale Anwendung; Protocol `CoreLoadResultLike` ([src/new_nfl/core/result.py](../src/new_nfl/core/result.py)) kapselt den gemeinsamen Result-Typ von Teams/Games/Players und ersetzt drei `isinstance`-Branches im CLI-Dispatch durch einen)
- T2.6G Provenance-Drilldown (neues Mart [src/new_nfl/mart/provenance.py](../src/new_nfl/mart/provenance.py) als `mart.provenance_v1` im Grain `(scope_type, scope_ref)`: UNION-ALL über sechs Core-Domänen `team` → `scope_ref=team_id`, `game` → `scope_ref=game_id`, `player` → `scope_ref=player_id`, `team_stats_weekly` → `scope_ref={team_id}:{season}:W{week:02d}`, `player_stats_weekly` → `scope_ref={player_id}:{season}:W{week:02d}`, `roster_membership` → `scope_ref={player_id}:{team_id}:{season}:W{week:02d}`, defensiv pro Domäne mit `_table_exists`-Guard (fresh DB liefert leere Projektion statt Exception); Source-Aggregation per `LIST(DISTINCT …) FILTER (WHERE … IS NOT NULL)` für `source_file_ids`/`source_adapter_ids`, `MIN(_canonicalized_at) AS first_seen_at`, `MAX(_canonicalized_at) AS last_canonicalized_at`, `COUNT(*) AS source_row_count`; Quarantäne-Aggregation per LEFT-JOIN auf `meta.quarantine_case` mit `COUNT(*) AS quarantine_case_count`, `COUNT(*) FILTER (WHERE status NOT IN ('resolved','closed','dismissed')) AS open_quarantine_count`, `ARG_MAX(reason_code, last_seen_at)` / `ARG_MAX(severity, …)` / `ARG_MAX(status, …)` / `MAX(last_seen_at) AS last_quarantine_at` für den jüngsten Case; abgeleitete Spalte `provenance_status` nach Kaskade `open_quarantine_count > 0 → 'warn'` · `source_row_count = 0 AND quarantine_case_count = 0 → 'unknown'` · sonst `'ok'`; scope_type/scope_ref zusätzlich als `*_lower`-Spalten für case-insensitive Reads. Read-Service [src/new_nfl/web/provenance_view.py](../src/new_nfl/web/provenance_view.py) liest ausschließlich aus `mart.provenance_v1` (ADR-0029): `list_provenance(settings, *, offset=0, limit=50, scope_type=None)` sortiert `open_quarantine_count DESC, last_canonicalized_at DESC NULLS LAST, scope_type, scope_ref` mit optionalem Scope-Type-Filter via `scope_type_lower = LOWER(?)` und Pagination im `ProvenanceListPage`-Bundle (Properties `has_prev`/`has_next`/`prev_offset`/`next_offset`/`page_range_label`); `get_provenance(settings, scope_type, scope_ref)` case-insensitive über beide `*_lower`-Spalten. `ProvenanceRecord` hat UX-abgeleitete Properties `source_file_count`, `adapter_count`, `primary_adapter` (erster Eintrag der Adapter-Liste), `status_label` (`OK` · `Quarantäne offen` · `Keine Provenienz`), `quarantine_label` (`—` bei 0 · `1 offen` / `2 offen` wenn open==total · `1 offen / 2 total` wenn gemischt · `2 geschlossen` wenn nur closed). Zwei neue Templates `provenance.html` (Index-Tabelle mit Spalten Scope-Typ/Scope-Ref/Adapter/Files/Quelle/Quarantäne/Status/Drilldown, `data-testid="provenance-row"` plus Scope-Type-Filter-Header) und `provenance_detail.html` (Header mit data-testid="provenance-header" + data-scope-type/-ref, Status-Badge, Source-File-Liste als Block-Spans, Quarantäne-Karte mit letztem reason_code/severity/status oder Empty-State bei null Cases). Navbar-Eintrag `Provenance` zwischen `Players` und `Runs` — Drilldown lebt neben den Domain-Views, noch vor Run-Evidence. Zwei neue Renderer-Entrypoints `render_provenance_page(settings, *, offset, limit, scope_type=None)` und `render_provenance_detail_page(settings, scope_type, scope_ref)` in [src/new_nfl/web/renderer.py](../src/new_nfl/web/renderer.py), Breadcrumbs `Home › Provenance` bzw. `Home › Provenance › {scope_type} › {scope_ref}`, `active_nav='provenance'`. Runner-Registry kennt `mart_key='provenance_v1'` (elif-Branch in `_executor_mart_build`). ADR-0029-Invariante bleibt erhalten: `tests/test_mart.py::READ_MODULES` erweitert um `src/new_nfl/web/provenance_view.py`. 16 neue Tests decken: Builder-Cold-Start (leere Projektion ohne Source-Tabellen), Build mit nur Core (fünf Scopes, alle `ok`), Build mit Source- und Quarantäne-Join (KC:2024:W01 hat source_row_count=1, open_quarantine_count=1, status=`warn`, last_reason_code=`points_mismatch`), Quarantäne-ohne-Source (Player `00-0012345` mit source_row_count=0 aber gelistet), list-Empty bei fehlendem Mart, list-Ordering (Warn-Scopes oben), Scope-Type-Filter case-insensitive, Pagination (`page_range_label='1–2 von 5'`), get case-insensitive, get-None bei Unknown, Quarantäne-Label-Format `1 offen / 2 total`, Render-Empty (`Noch keine Provenienz-Zeilen`), Render mit Rows (`provenance-row`-Testid), Render-404 (`Scope nicht gefunden`), Render-Detail-Happy (`sf-tsw-001` + `points_mismatch` im HTML), Render-mit-Scope-Type-Filter. Full-Suite 300/300 grün, Ruff auf geänderten Files sauber)
- T2.6F Game-Detail Pre/Post (Read-Service [src/new_nfl/web/game_view.py](../src/new_nfl/web/game_view.py) orchestriert vier Mart-Zugriffe in einem Dataclass-Bundle: `get_game_detail(settings, game_id)` macht case-insensitive Lookup über `mart.game_overview_v1.game_id_lower`, LEFT-JOIN auf `mart.team_overview_v1` via `home_team_lower`/`away_team_lower` (`team_id_lower`) für `home_team_name`/`away_team_name`; Pre/Post-Entscheidung folgt `meta.is_completed`: Pre-Game lädt ausschließlich die Form beider Teams aus `mart.team_stats_weekly_v1` aggregiert über `WHERE season = ? AND week < ? AND LOWER(team_id) = LOWER(?) AND point_diff IS NOT NULL` mit `SUM(CASE WHEN point_diff > 0 THEN 1 ELSE 0 END) AS wins` usw.; Post-Game lädt zusätzlich die Wochenzeile beider Teams (`week = ?`) und den Top-10-Boxscore je Team aus `mart.player_stats_weekly_v1` per `ORDER BY total_yards DESC NULLS LAST, total_touchdowns DESC NULLS LAST, display_name NULLS LAST LIMIT 10`. `GameMeta` hat UX-abgeleitete Properties `score_label` (`27 – 20` oder `—` bei NULL-Scores), `status_label` (`Final` · `Final (OT)` bei `overtime > 0` · `geplant` bei `is_completed=False`), `kickoff_label` (`2024-09-15 · 20:20`), `venue_label` (`GEHA Field · outdoors · grass`), `matchup_label` (`BAL @ KC` für Browser-Title + Breadcrumb), `home_label`/`away_label` (Team-Name mit Fallback auf Abbr), `winner_label` (`Kansas City Chiefs` · `Unentschieden` bei `winner_team='TIE'` · `—` bei pre-game). `TeamSideForm` mit `record_label` (`5–3` oder `5–3–1`) und `avg_points_for`/`avg_points_against`. `BoxscorePlayer.display_label` Fallback-Kaskade display_name → player_id. Ein neues Template `game_detail.html` rendert: 404-Karte bei `detail is None`, Header mit Score/Status/Kickoff/Venue, bei Pre-Game zwei nebeneinander liegende Form-Karten, bei Post-Game zwei Team-Line-Karten plus zwei Boxscore-Tabellen. Neue Renderer-Entrypoint `render_game_detail_page(settings, game_id)` in [src/new_nfl/web/renderer.py](../src/new_nfl/web/renderer.py), Breadcrumb `Home › Seasons › Season N › Woche W › {matchup_label}`, `active_nav='seasons'` (Drilldown vom Wochen-Games-View). ADR-0029-Invariante bleibt erhalten: `tests/test_mart.py::READ_MODULES` erweitert um `src/new_nfl/web/game_view.py`. 13 neue Tests decken Empty-State je Mart (kein Game-Mart → None, kein Team-Overview → Team-Name None aber Meta ok, kein team_stats_weekly → form/week None, kein player_stats_weekly → Boxscore leer), Pre- vs. Post-Branch, Score-/Status-/Winner-Label inkl. TIE → `Unentschieden`, OT-Suffix, case-insensitive Lookup, Form-Aggregation (W-L aus `point_diff`-Zeichen über week<current), Top-N-Ordering und Template-Rendering mit Breadcrumb-Chain ab)
- T2.5D Rosters-Domäne (erste bitemporale Domäne) (Slices `(nflverse_bulk, rosters)` primär und `(official_context_web, rosters)` als Cross-Check; `core.roster_membership` ([src/new_nfl/core/rosters.py](../src/new_nfl/core/rosters.py)) mit bitemporalen Intervall-Spalten `valid_from_week`/`valid_to_week` neben System-Time `_loaded_at`; Interval-Bau über CTE-Kaskade mit `week - ROW_NUMBER() OVER (PARTITION BY player_id, team_id, season, position, jersey_number, status ORDER BY week)`-Gap-Trick und `valid_to_week IS NULL` gdw. `raw_valid_to_week >= global_max_week` pro Saison; `meta.roster_event` schreibt pro Intervall-Transition `signed`/`released`/`trade`/`promoted`/`demoted` (Python-seitige Scan-Phase, idempotent per DELETE-before-INSERT pro Saison); Trade-Heuristik konservativ — zusammenhängende Woche mit Team-Wechsel → `trade`, Lücke → `released` + `signed`; Tier-B-Diskrepanzen auf `position`, `jersey_number`, `status` im Grain `(player_id, team_id, season, week)` öffnen `meta.quarantine_case` mit `scope_type='roster_membership'` und `scope_ref=PLAYER:TEAM:SEASON:Wxx`; zwei Read-Modelle `mart.roster_current_v1` ([src/new_nfl/mart/roster_current.py](../src/new_nfl/mart/roster_current.py)) mit `WHERE valid_to_week IS NULL` und `mart.roster_history_v1` ([src/new_nfl/mart/roster_history.py](../src/new_nfl/mart/roster_history.py)) mit vollem Timeline + abgeleitetem `is_open`; beide Marts mit best-effort LEFT-JOIN auf `core.player`/`core.team` (Display-Name, Team-Name/Abbr), Fallback auf NULL wenn Quellen nicht vorhanden; `CoreRosterLoadResult` erfüllt `CoreLoadResultLike`-Protocol (Round-Trip-Test `isinstance(result, CoreLoadResultLike)`); ADR-0032 `Proposed`)
- T2.6E Player-Profil (Read-Service [src/new_nfl/web/player_view.py](../src/new_nfl/web/player_view.py) orchestriert vier Mart-Zugriffe in einem Dataclass-Bundle: `list_players(settings, offset=0, limit=50)` liefert eine paginierte Liste aus `mart.player_overview_v1` sortiert `is_active DESC, last_season DESC NULLS LAST, display_name, player_id` mit LEFT-JOIN auf `mart.team_overview_v1` (`team_id_lower`) für `current_team_abbr` — fällt bei fehlender Team-Mart auf einen Plain-Select ohne Team-Anreicherung zurück; `PlayerListPage`-Dataclass kapselt `players`, `offset`, `limit`, `total` plus `has_prev`/`has_next`/`prev_offset`/`next_offset`/`page_range_label` für das Template; `get_player_profile(settings, player_id)` macht case-insensitive Lookup über `player_id_lower`, lädt Stammdaten aus `mart.player_overview_v1` (mit Team-Join für `current_team_abbr`/`current_team_name` via `current_team_id_lower`), Karriere-Snapshot aus `mart.player_stats_career_v1` (`LIMIT 1`), Saison-Historie aus `mart.player_stats_season_v1` DESC und Roster-Intervalle aus `mart.roster_history_v1` DESC nach `season, valid_from_week, team_id`. `PlayerMeta` hat UX-abgeleitete Properties `display_label` (Fallback-Kaskade display_name → full_name → player_id), `height_label` (`74` → `6'2"`), `weight_label` (`225` → `225 lbs`), `draft_label` (`2017 · R1·P10 · KC`), `seasons_label` (`2017–heute` für offen, `2000–2022` für geschlossen), `jersey_label` (`#15` oder `—`). `PlayerRosterInterval.week_range_label` rendert `W1–offen` für offene Intervalle und `W1–W17` für geschlossene (basierend auf ADR-0032-`is_open`-Flag). Zwei neue Templates `players.html`/`player_profile.html` erweitern das Komponenten-Set aus T2.6A; Player-Liste mit `data-testid="player-row"` + `data-player-id`, Profile mit vier Karten (Stammdaten als `data-table`, Karriere-Totale als `data-table`, Saison-Historie als klassische Tabelle, Roster-Historie mit Offen/Geschlossen-Badge). Pagination-Links unter der Player-Liste mit `data-testid="player-pagination"` und `offset`/`limit`-Query-Params; Empty-State-Pfad bei fehlender Mart und 404-Karte bei `profile is None`. Zwei neue Renderer-Entrypoints `render_players_page`/`render_player_profile_page` in [src/new_nfl/web/renderer.py](../src/new_nfl/web/renderer.py), Breadcrumb `Home › Players` bzw. `Home › Players › {display_label}`, `active_nav='players'`. ADR-0029-Invariante bleibt erhalten: `tests/test_mart.py::READ_MODULES` erweitert um `src/new_nfl/web/player_view.py`. 19 neue Tests decken Empty-State je Mart, Ordering (is_active DESC → last_season DESC NULLS LAST → display_name), Team-Join und Fallback ohne Team-Mart, Pagination mit Offset/Limit, case-insensitive Lookup, Career-Span-Label, Height-Format, Draft-Label, Open-Intervall, Breadcrumb-Chain und 404-Rendering ab)
- T2.6D Team-Profil (Read-Service [src/new_nfl/web/team_view.py](../src/new_nfl/web/team_view.py) orchestriert vier Mart-Zugriffe in einem Dataclass-Bundle: `list_teams(settings)` liefert alle `mart.team_overview_v1`-Zeilen mit LEFT-JOIN auf die neueste Saison-Stats-Zeile per `ROW_NUMBER() OVER (PARTITION BY team_id ORDER BY season DESC)` — fällt bei fehlender `mart.team_stats_season_v1` auf einen Plain-Select mit NULL-Stats zurück statt leerer Seite; `get_team_profile(settings, team_key, season=None)` macht case-insensitive Lookup über `team_id_lower`/`team_abbr_lower`, lädt das Roster aus `mart.roster_current_v1` (ORDER BY position, jersey_number, display_name — offene Intervalle per Mart-Definition), die Saison-Historie aus `mart.team_stats_season_v1` DESC, und die Spielhistorie aus `mart.game_overview_v1` gefiltert auf `home_team_lower = ? OR away_team_lower = ?` in der gewählten Saison; fehlt die Saison aus der URL, wird auf `available_seasons[0]` zurückgefallen, bei leerem Available auf `None`. `TeamGameRow` dreht das Spiel aus Team-Perspektive (`is_home`, `opponent`, `score_for`/`score_against`, `outcome ∈ {win,loss,tie,scheduled}`); `TeamProfile.record_label` rechnet daraus W–L oder W–L–T. Zwei neue Templates `teams.html`/`team_profile.html` erweitern das Komponenten-Set aus T2.6A; Stammdaten-Karte rendert als `data-table` (ohne neue CSS-Klasse), Roster-/Stats-/History-Karten als klassische Tabellen mit `data-testid="roster-row"`/`season-stats-row"`/`team-game-row"` plus `data-player-id`/`data-game-id` für htmx-Targets. Saison-Picker unterhalb der Games-Karte listet `available_seasons` als Links mit aktiver Saison in `font-semibold`. Empty-State-Pfad: `TeamProfile is None` rendert eine eigene `Team nicht gefunden`-Karte mit dem angefragten Key als Hint. Zwei neue Renderer-Entrypoints `render_teams_page`/`render_team_profile_page` in [src/new_nfl/web/renderer.py](../src/new_nfl/web/renderer.py), Breadcrumb `Home › Teams` bzw. `Home › Teams › {team_name}`, `active_nav='teams'`. ADR-0029-Invariante bleibt erhalten: `tests/test_mart.py::READ_MODULES` erweitert um `src/new_nfl/web/team_view.py`. 19 neue Tests decken Empty-States je Mart, case-insensitive Lookup, Roster-Ordering, Season-Stats-DESC, Game-Perspektive-Inversion, Record-Label, Saison-Default vs. explizit, 404-Rendering und Breadcrumb-Chain ab)
- T2.6C Season/Week/Game-Drilldown (Read-Service [src/new_nfl/web/games_view.py](../src/new_nfl/web/games_view.py) liest `mart.game_overview_v1` in drei Schichten: `list_seasons` per `GROUP BY season` mit `game_count`/`completed_count`/`min_week`/`max_week`, `list_seasons` sortiert DESC damit die neueste Saison oben steht; `list_weeks(settings, season)` per `GROUP BY season, week` mit `earliest_gameday`; `list_games(settings, season, week)` per Plain-Selects mit `ORDER BY gameday NULLS LAST, gametime NULLS LAST, home_team`. Drei neue Templates `seasons.html`/`season_weeks.html`/`week_games.html` erweitern das Komponenten-Set aus T2.6A — alle rendern über `card`+`empty_state`, der Breadcrumb-Pfad baut sich inkrementell auf (`Home › Seasons`, `Home › Seasons › Season N`, `Home › Seasons › Season N › Woche W`). Drei neue Renderer-Entrypoints `render_seasons_page`/`render_season_weeks_page`/`render_week_games_page` in [src/new_nfl/web/renderer.py](../src/new_nfl/web/renderer.py). `GameRow.score_label` liefert bei Null-Scores `—` statt `0 – 0`, `GameRow.status` kollabiert `is_completed` auf `'final'`/`'scheduled'` für die Badge. Read-Surface-Invariante (ADR-0029) bleibt erhalten: `tests/test_mart.py::READ_MODULES` und `tests/test_web_fundament.py` AST-Lint erweitert um `src/new_nfl/web/games_view.py`. 15 neue Tests decken Cold-Start (Empty-Tuple statt Exception), Season-Ordering, Week-Filter, Game-Ordering, Score-Dash für Unplayed, Breadcrumb-Chain und `active_nav='seasons'` ab)
- T2.6B Home/Freshness-Dashboard (`mart.freshness_overview_v1` ([src/new_nfl/mart/freshness_overview.py](../src/new_nfl/mart/freshness_overview.py)) aggregiert `meta.load_events` + `meta.quarantine_case` über die sechs erwarteten Core-Domänen (`team`, `game`, `player`, `roster_membership`, `team_stats_weekly`, `player_stats_weekly`): letzter `core_loaded`-Event je Domäne via `ARG_MAX(…, recorded_at)` plus `open_quarantine_count` aus `meta.quarantine_case WHERE status NOT IN ('resolved','closed','dismissed')`; abgeleitete `freshness_status`-Spalte mit Kaskade `stale → fail → warn → ok`; Cold-Start-Sicherheit durch `expected_domains`-CTE, sodass die Projektion auch bei leeren Metadaten-Tabellen sechs Zeilen liefert; Read-Service [src/new_nfl/web/freshness.py](../src/new_nfl/web/freshness.py) mit `HomeOverview`-Dataclass (Totals, Domänen-Buckets `ok`/`warn`/`stale`/`fail`), fängt fehlende Mart-Tabelle ab und liefert synthetische `stale`-Zeilen; `render_home_from_settings` in [src/new_nfl/web/renderer.py](../src/new_nfl/web/renderer.py) rendert die echte Freshness-Liste je Domäne plus Q-Badge wenn `open_quarantine_count > 0`; Runner-Registry kennt `freshness_overview_v1` als Mart-Key; AST-Lint-Test und `test_mart.py READ_MODULES` erweitert um `src/new_nfl/web/freshness.py`, ADR-0029-Invariante bleibt wahr)
- T2.6A UI-Fundament ([src/new_nfl/web/](../src/new_nfl/web/): Jinja2-basierter Renderer [src/new_nfl/web/renderer.py](../src/new_nfl/web/renderer.py) mit `WebRenderer`-Dataclass und `build_renderer`-Factory; `StaticAssetResolver` in [src/new_nfl/web/assets.py](../src/new_nfl/web/assets.py) als Single-Source-of-Truth für `/static/`-Pfade; Template-Hierarchie mit `base.html` + `home.html` und Komponenten-Macros unter `templates/_components/` für Navbar, Breadcrumb, Card, StatTile, DataTable, FreshnessBadge, EmptyState; Tailwind-kompatibler CSS-Subset [src/new_nfl/web/static/css/app.css](../src/new_nfl/web/static/css/app.css) mit CSS-Custom-Properties für Dark/Light-Tokens (Zinc-Palette + Emerald-Akzent nach UI_STYLE_GUIDE §3); Theme-Bootstrap inline im `<head>` (Pre-FOUC), Persistenz in `localStorage`, initial aus `prefers-color-scheme`; Lucide-Sprite-Skelett unter `static/icons/lucide-sprite.svg`; Jinja-Filter `relative_time`/`fmt_number` für deutsche Zeitangaben und Non-Breaking-Thousands-Separator; ADR-0030 final `Accepted` mit Implementierungs-Notizen zur Tailwind-Subset-vs-CLI-Entscheidung; AST-Lint-Test (`mart.*`-only) erweitert auf die drei neuen Web-Python-Module; Jinja2 als Pflicht-Dependency in `pyproject.toml` plus `include-package-data` für Templates und statische Assets)
- T2.5F Player-Stats-Aggregate (zweite aggregierende Domäne) (Slices `(nflverse_bulk, player_stats_weekly)` primär und `(official_context_web, player_stats_weekly)` als Cross-Check; `core.player_stats_weekly` ([src/new_nfl/core/player_stats.py](../src/new_nfl/core/player_stats.py)) im Grain `(season, week, player_id)` mit `UPPER(TRIM(player_id))`-Kanonicalisierung und TRY_CAST auf `season`/`week`/`team_id`/`position`/`passing_yards`/`passing_tds`/`interceptions`/`rushing_yards`/`rushing_tds`/`receptions`/`receiving_yards`/`receiving_tds`/`touchdowns`/`fumbles_lost`; Dedupe über `ROW_NUMBER() OVER (PARTITION BY season, week, player_id ORDER BY _loaded_at DESC NULLS LAST, _source_file_id DESC)` — letzter Load gewinnt; Tier-B-Cross-Check auf `passing_yards`/`rushing_yards`/`receiving_yards`/`touchdowns` öffnet `meta.quarantine_case` mit `scope_type='player_stats_weekly'` und `scope_ref={player_id}:{season}:W{week:02d}`; drei Read-Modelle `mart.player_stats_weekly_v1` ([src/new_nfl/mart/player_stats_weekly.py](../src/new_nfl/mart/player_stats_weekly.py)) als Passthrough mit abgeleitetem `total_yards`/`total_touchdowns`, `mart.player_stats_season_v1` ([src/new_nfl/mart/player_stats_season.py](../src/new_nfl/mart/player_stats_season.py)) als GROUP BY `(season, player_id)`-Aggregat mit `MODE(position) AS primary_position` und `COUNT(CASE WHEN <has-any-stat>)` für bye-tolerante `games_played` (Multi-Position-Tolerance: Taysom Hill QB/TE/RB aggregiert positions-agnostisch) und `mart.player_stats_career_v1` ([src/new_nfl/mart/player_stats_career.py](../src/new_nfl/mart/player_stats_career.py)) als GROUP BY `player_id`-Aggregat über alle Saisons mit `MIN(season)`/`MAX(season)` und `COUNT(DISTINCT CASE WHEN <has-any-stat> THEN season END) AS seasons_played`; alle drei Marts mit best-effort LEFT-JOIN auf `core.player`/`core.team` (display_name, team_name/abbr), Fallback auf NULL wenn Quelle nicht vorhanden; `CorePlayerStatsLoadResult` erfüllt `CoreLoadResultLike`-Protocol)
- T2.5E Team-Stats-Aggregate (erste aggregierende Domäne) (Slices `(nflverse_bulk, team_stats_weekly)` primär und `(official_context_web, team_stats_weekly)` als Cross-Check; `core.team_stats_weekly` ([src/new_nfl/core/team_stats.py](../src/new_nfl/core/team_stats.py)) im Grain `(season, week, team_id)` mit `UPPER(TRIM(team_id))`-Kanonicalisierung und TRY_CAST auf `season`/`week`/`points_for`/`points_against`/`yards_for`/`yards_against`/`turnovers`/`penalties_for`; Dedupe über `ROW_NUMBER() OVER (PARTITION BY season, week, team_id ORDER BY _loaded_at DESC NULLS LAST, _source_file_id DESC)` — letzter Load gewinnt bei Reprocessing; Tier-B-Cross-Check auf `points_for`/`points_against`/`yards_for`/`turnovers` öffnet `meta.quarantine_case` mit `scope_type='team_stats_weekly'` und `scope_ref={team_id}:{season}:W{week:02d}`; zwei Read-Modelle `mart.team_stats_weekly_v1` ([src/new_nfl/mart/team_stats_weekly.py](../src/new_nfl/mart/team_stats_weekly.py)) als Passthrough mit abgeleitetem `point_diff`/`yard_diff` und `mart.team_stats_season_v1` ([src/new_nfl/mart/team_stats_season.py](../src/new_nfl/mart/team_stats_season.py)) als GROUP BY `(season, team_id)`-Aggregat mit `COUNT(points_for) AS games_played` (Bye-Week-Toleranz: fehlende Wochen-Zeile bläht `games_played` nicht auf); beide Marts mit best-effort LEFT-JOIN auf `core.team` (Name/Abbr), Fallback auf NULL wenn Quelle nicht vorhanden; `CoreTeamStatsLoadResult` erfüllt `CoreLoadResultLike`-Protocol)

## Current runtime posture

- local Python package with CLI surface
- local DuckDB metadata surface
- seeded source registry
- adapter catalog
- raw landing receipts
- true remote fetch path with dry-run and execute modes
- source-file discovery and explicit `source_file_id` pinning
- first normalized staging load into `stg.nflverse_bulk_schedule_dictionary`
- first canonical core load into `core.schedule_field_dictionary`
- browse / exact lookup / summary over the core dictionary
- local HTML preview export
- local mini webserver for preview
- interner Job-/Run-Modell-Store in DuckDB (meta.job_*, meta.retry_policy, meta.run_event, meta.run_artifact) mit Pydantic-Modellen und CLI-Oberfläche
- interner Job-Runner: atomares Claim auf `meta.job_queue`, Executor-Registry (`fetch_remote`, `stage_load`, `custom`), Retry-Policy-Auswertung, deterministischer Replay gescheiterter Runs; CLI `run-worker --once|--serve` und `replay-run --job-run-id`; `fetch-remote` und `stage-load` erzeugen nur noch über den Runner Evidence (Manifest §3.9, §3.13)
- First-class Quarantäne-Domäne: jeder `runner_exhausted`-Run öffnet einen `meta.quarantine_case` mit Evidence-Ref; Operator-Aktionen (`replay`, `override`, `suppress`) werden als `meta.recovery_action` persistiert und verlinken bei Replay den neuen `job_run_id` (ADR-0028)
- Read-Modell-Schicht `mart.*` als einziger Lesepfad für CLI-Browse/Web-Preview (ADR-0029): `mart.schedule_field_dictionary_v1` voll rebuildbar aus `core.*`, automatisch nach `core-load --execute` aufgefrischt, separat über CLI `mart-rebuild` als Runner-Job; Direktzugriffe aus Read-Modulen auf `core.*`/`stg.*`/`raw/` werden durch einen Lint-Test blockiert
- Ontologie-as-Code v0_1 (ADR-0026): TOML-Quellen unter `ontology/v0_1/` (Position, Game-Status, Injury-Status), Loader idempotent über `content_sha256`, Projektion in `meta.ontology_*`; CLI `ontology-load`/`ontology-list`/`ontology-show` mit Alias-Auflösung; `meta.ontology_version` markiert die aktive Version pro Quellverzeichnis
- Dedupe-Pipeline-Skelett (ADR-0027): fünf explizite Stufen unter `src/new_nfl/dedupe/`, regel-basierter Scorer mit Pluggable-Interface, Auto-Merge ab `score >= 0.85`, Review-Queue für `0.50 <= score < 0.85`; Evidence in `meta.dedupe_run`; offene Pairs in `meta.review_item`; CLI `dedupe-run --domain players --demo` und `dedupe-review-list`
- Adapter-Slice-Registry (ADR-0031 Accepted seit T2.5B): ein `adapter_id` kann mehrere Slices bedienen; slice-spezifische `remote_url`, `stage_target_object`, `core_table`, `mart_key` + Rolle (`primary`/`cross_check`); Registry nach T2.5F: `(nflverse_bulk, schedule_field_dictionary)`, `(nflverse_bulk, teams)`, `(nflverse_bulk, games)`, `(nflverse_bulk, players)`, `(nflverse_bulk, rosters)`, `(nflverse_bulk, team_stats_weekly)`, `(nflverse_bulk, player_stats_weekly)` als Primary plus `(official_context_web, teams)`, `(official_context_web, games)`, `(official_context_web, players)`, `(official_context_web, rosters)`, `(official_context_web, team_stats_weekly)`, `(official_context_web, player_stats_weekly)` als Cross-Check; Quarantäne-Hook auf Tier-A/B-Diskrepanz in `core.team`, `core.game`, `core.player`, `core.roster_membership`, `core.team_stats_weekly`, `core.player_stats_weekly`; Operator-CLI `list-slices` für Registry-Sicht
- `core.game` slice-zentrisch aus `(nflverse_bulk, games)` mit Tier-A-Dominanz, `mart.game_overview_v1` als Read-Projektion (abgeleitet: `is_completed`, `winner_team`), `(official_context_web, games)` als erste reale HTTP-getriebene Tier-B-Quelle
- `core.player` slice-zentrisch aus `(nflverse_bulk, players)` mit `UPPER(TRIM(player_id))`-Kanonicalisierung, `mart.player_overview_v1` als Read-Projektion (abgeleitet: `full_name`, `is_active`, `position_is_known`) mit best-effort Ontologie-Lookup gegen die aktive `position`-Value-Set-Version, Dedupe-Pipeline (ADR-0027) liest `core.player` direkt für die erste reale Anwendung (CLI `dedupe-run --source core-player`), gemeinsamer Result-Typ `CoreLoadResultLike` (Protocol) für Teams/Games/Players
- `core.roster_membership` als erste bitemporale Domäne (ADR-0032 Proposed): `(player_id, team_id, season, valid_from_week, valid_to_week)` mit Business-Time-Intervallen, `valid_to_week IS NULL` für offene Mitgliedschaft; `meta.roster_event` als Event-Stream mit `signed`/`released`/`trade`/`promoted`/`demoted`; Trade-Heuristik konservativ (lücken-frei → `trade`, Lücke → `released`+`signed`); zwei Read-Modelle `mart.roster_current_v1` (nur offene Intervalle) und `mart.roster_history_v1` (volle Timeline mit `is_open`); CoreLoad-Dispatch erkennt `core.roster_membership` und routet auf `execute_core_roster_load`
- `core.team_stats_weekly` als erste aggregierende Domäne: Grain `(season, week, team_id)`, Dedupe per `ROW_NUMBER() … ORDER BY _loaded_at DESC` (letzter Load gewinnt), Tier-B-Cross-Check auf `points_for`/`points_against`/`yards_for`/`turnovers`; Read-Modelle `mart.team_stats_weekly_v1` (Passthrough + abgeleitete Differenzen) und `mart.team_stats_season_v1` (Saison-Aggregat mit `COUNT(points_for)` → bye-week-tolerant); CoreLoad-Dispatch routet `core.team_stats_weekly` auf `execute_core_team_stats_load`, Runner-Registry kennt `team_stats_weekly_v1` und `team_stats_season_v1` als Mart-Keys
- UI-Fundament (ADR-0030 Accepted seit T2.6A): Jinja2-basierter Server-Renderer unter `src/new_nfl/web/`, CSS-Custom-Property-Tokens für Dark/Light mit einem Attribute-Swap auf `<html data-theme>`, Tailwind-kompatibler Utility-Subset (hand-assembliert, ohne Node-Toolchain-Zwang), Komponenten-Macros für Card/StatTile/DataTable/FreshnessBadge/Navbar/Breadcrumb/EmptyState, Theme-Toggle via `[data-theme-toggle]`-Event-Delegation; AST-Lint auf `mart.*`-only erweitert auf `src/new_nfl/web/`
- Season/Week/Game-Drilldown (T2.6C): zweite und dritte Navigationsebene über `mart.game_overview_v1`; `/seasons` listet alle Saisons DESC mit Abgeschlossen-Count und Wochen-Range, `/seasons/<s>` die Wochen der Saison mit `earliest_gameday`, `/seasons/<s>/weeks/<w>` die einzelnen Games mit Score/Status/Venue; Empty-States je Ebene mit CLI-Hint; Breadcrumb-Chain inkrementell; AST-Lint auf `mart.*`-only erweitert auf den neuen Read-Service
- Provenance-Drilldown (T2.6G): neunte Navigationsebene `/provenance` plus Detail `/provenance/<scope_type>/<scope_ref>`; `mart.provenance_v1` projiziert pro Scope-Grain Source-Coverage (aus sechs Core-Domänen via defensive UNION-ALL mit `_table_exists`-Guard) und Quarantäne-Aggregate (aus `meta.quarantine_case` mit `ARG_MAX` auf `last_seen_at` für den jüngsten Case); abgeleiteter `provenance_status` (`warn` bei offenen Cases, `unknown` bei weder Source noch Quarantäne, sonst `ok`); Index sortiert Warn-Scopes oben, unterstützt Scope-Type-Filter (case-insensitive) und Pagination; Detail zeigt alle source_file_ids, Adapter-Liste, Kanonisierungs-Zeitstempel und die letzte Quarantäne-Zusammenfassung; Navbar-Eintrag zwischen Players und Runs — Drilldown lebt neben den Domain-Views, noch vor Run-Evidence; AST-Lint (ADR-0029) auf `mart.*`-only erweitert auf den neuen Read-Service
- Game-Detail (T2.6F): achte Navigationsebene unter Seasons › Woche; `/games/<game_id>` rendert pro Spiel entweder eine Pre-Game-Ansicht (beide Teams mit `record_label` + avg Points-For/Against aggregiert aus `mart.team_stats_weekly_v1` über `week < current`) oder eine Post-Game-Ansicht (finale Score mit `Final`/`Final (OT)`-Badge, pro Team eine Wochenzeile aus `mart.team_stats_weekly_v1` plus Top-10-Boxscore aus `mart.player_stats_weekly_v1` per `total_yards DESC LIMIT 10`); Pre/Post-Split folgt `meta.is_completed`; Team-Namen kommen per LEFT-JOIN aus `mart.team_overview_v1`, `winner_team='TIE'` rendert als `Unentschieden`; jede Mart-Abhängigkeit degradiert unabhängig (fehlendes `team_stats_weekly` → form/week None aber Header ok, fehlendes `player_stats_weekly` → Boxscore leer, fehlendes `team_overview` → Team-Name fällt auf Abbr); Empty-States je Karte mit CLI-Hint; Breadcrumb `Home › Seasons › Season N › Woche W › {matchup_label}`, `active_nav='seasons'`; `GameDetail is None` rendert eigene 404-Karte
- Player-Profil (T2.6E): sechste und siebte Navigationsebene; `/players` listet paginiert (Default 50 pro Seite, `?offset=`/`?limit=`) alle Spieler aus `mart.player_overview_v1` sortiert `is_active DESC, last_season DESC NULLS LAST, display_name, player_id` mit LEFT-JOIN auf `mart.team_overview_v1` für `current_team_abbr` (Fallback Plain-Select ohne Team-Mart); `/players/<player_id>` rendert das Profil in vier Karten (Stammdaten, Karriere-Totale aus `mart.player_stats_career_v1`, Saison-Historie aus `mart.player_stats_season_v1` DESC, Roster-Historie aus `mart.roster_history_v1` DESC mit `offen`/`geschlossen`-Badge pro Intervall); UX-Properties im Service (`height_label` inch→ft'in", `draft_label`, `seasons_label`, `week_range_label` inkl. `W1–offen` für offene Intervalle) entkoppeln die Template-Schicht von Datenformat-Details; Empty-States je Karte mit CLI-Hint; Breadcrumb `Home › Players › {display_label}`, `active_nav='players'`; `PlayerProfile is None` rendert eigene 404-Karte
- Team-Profil (T2.6D): vierte und fünfte Navigationsebene; `/teams` listet alle Teams aus `mart.team_overview_v1` sortiert nach Conference → Division → Name mit LEFT-JOIN auf die neueste `mart.team_stats_season_v1`-Zeile (PF/PA/Latest-Season) — fällt ohne Stats-Tabelle auf einen Plain-Select zurück; `/teams/<abbr>` rendert das Profil in vier Karten (Stammdaten, aktuelles Roster aus `mart.roster_current_v1`, Saison-Historie aus `mart.team_stats_season_v1` DESC, Spiel-Historie aus `mart.game_overview_v1` aus Team-Perspektive mit `W`/`L`/`T`/`geplant`-Badges); Saison-Picker unterhalb der Spiele; Empty-States je Karte mit CLI-Hint; Breadcrumb `Home › Teams › {team_name}`, `active_nav='teams'`; `TeamProfile is None` rendert eigene 404-Karte
- Home/Freshness-Dashboard (T2.6B): erste echte UI-View, die über `mart.freshness_overview_v1` je Core-Domäne `last_event_at`/`event_count`/`open_quarantine_count`/`freshness_status` rendert; Service-Layer [src/new_nfl/web/freshness.py](../src/new_nfl/web/freshness.py) liest ausschließlich aus `mart.*` und fällt bei fehlender Mart-Tabelle auf synthetische `stale`-Zeilen zurück; StatTiles zeigen Domänen-Verteilung (`grün`/`stale`/`warn+fail`/`offene Quarantäne`); Runner-Registry kennt `mart_key='freshness_overview_v1'`
- `core.player_stats_weekly` als zweite aggregierende Domäne: Grain `(season, week, player_id)`, Dedupe per `ROW_NUMBER() … ORDER BY _loaded_at DESC`, Tier-B-Cross-Check auf `passing_yards`/`rushing_yards`/`receiving_yards`/`touchdowns`; drei Read-Modelle `mart.player_stats_weekly_v1` (Passthrough + abgeleitete `total_yards`/`total_touchdowns`), `mart.player_stats_season_v1` (Saison-Aggregat mit `MODE(position) AS primary_position`, Multi-Position-tolerant, bye-tolerant via `COUNT(CASE WHEN <has-any-stat>)`) und `mart.player_stats_career_v1` (Karriere-Aggregat über alle Saisons mit `MIN/MAX(season)` und `COUNT(DISTINCT CASE WHEN <has-any-stat> THEN season END)` für `seasons_played`); CoreLoad-Dispatch routet `core.player_stats_weekly` auf `execute_core_player_stats_load`, Runner-Registry kennt `player_stats_weekly_v1`, `player_stats_season_v1`, `player_stats_career_v1` als Mart-Keys

## Current release posture

The project now has a **local preview-release candidate**.

That means:
- data can be fetched and loaded locally
- the first canonical core object can be built locally
- the first web-facing preview can be rendered and served locally

What is still missing before the first VPS preview release:
- a pinned VPS runbook with exact operator steps
- a preview release cut that is replayed on the VPS
- a VPS smoke test covering `/healthz` and `/`
- an explicit rollback / restart note for the preview service

## Current cycle

T2.2 (lokales Preview + VPS-Runbook) ist abgeschlossen. **VPS-Deploy ist auf nach v1.0 verschoben** (Operator-Entscheidung, siehe `concepts/NEW_NFL_SYSTEM_CONCEPT_v0_3.md` §2). Der Fokus wechselt auf interne Foundation-Härtung, damit alle Phase-1-Domänen autonom, evidenz- und replay-fähig laufen.

## Preferred next bolt

**T2.6H — Run-Evidence-Browser** gemäß `T2_3_PLAN.md` §5: fünfte (und letzte) UI-Pflicht-View der Tranche vor T2.7. Ziel ist, dass der Operator vom Provenance-Detail (oder direkt von `/runs`) zu den erzeugenden Run-Records springen kann — Lesepfad muss weiterhin aus `mart.*` kommen (ADR-0029), also wahrscheinlich neues `mart.run_overview_v1` als Projektion über `meta.job_run` + `meta.run_event` + `meta.run_artifact`. Offene Entscheidungen: welche Events gruppieren (nur Startup/Completion? oder alle `recorded_at`-Events?), welche Artefakte verlinken (nur text? blob-downloads?), und ob ein Quarantäne-Case mit `job_run_id` den Rückweg zum Run öffnet. Risiken: `meta.run_event` wächst schnell — Pagination + Retention sind Pflicht. Folgebolzen: Final-Handoff + Starter-Prompt T2.7 (Rückweg der Phase-1-Domänen in Produktion).

## Zielkorridor v1.0

- feature-complete: Ende Juni 2026
- Testphase: Juli 2026
- produktiv (auf Windows-VPS): vor Preseason-Start Anfang August 2026
